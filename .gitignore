node_modules/
coverage/
*.log
.DS_Store

# The build output is not kept in the repository. CI builds it on every push
# and publishes it to GitHub Pages; the single self-contained file is also
# attached to each run as a downloadable artifact.
dist/
